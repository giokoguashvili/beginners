{
  int abcd;
  abcd = 2345;
  
  int ab;
  int AB;
  
  ab = (abcd / 100); 
  AB = (ab / 10) + (ab % 10); 
  
  int cd;
  int CD;
  
  cd = (abcd % 100);
  CD = (cd / 10) + (cd % 10);
  
  int ABCD;
  ABCD = AB + CD;
}
